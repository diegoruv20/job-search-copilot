# Claude Code adapter

Read and follow `AGENTS.md` as the authoritative repository contract before
acting. Client-specific behavior must not weaken its safety, privacy, workflow,
or contribution rules.

Use the relevant portable playbook under `.github/skills/` for the user's task.
Before personalized job-search work, inspect the live tracker through the
`job-search-copilot` MCP server and use Playwright for human-visible websites.
